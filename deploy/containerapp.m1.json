{
  "artifactClass": "web-with-backend",
  "containerPort": 8080,
  "scale": {
    "minReplicas": 1,
    "maxReplicas": 1
  },
  "reason": "The release uses one migration-managed SQLite store, one persisted contact-encryption key, and process-local rate buckets. Keep one ingress-routed replica so owner data, callbacks, and the 12-write boundary remain service-wide."
}
