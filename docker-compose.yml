services:
  app:
    build:
      context: .
      args:
        BUILD_SHA: local-compose
    environment:
      PORT: "8080"
    ports:
      - "8080:8080"
    volumes:
      - booking-recovery-loop-data:/data

volumes:
  booking-recovery-loop-data:
